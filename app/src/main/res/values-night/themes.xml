<resources xmlns:tools="http://schemas.android.com/tools" xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Base application theme. -->
    <style name="Theme.Calculator" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color -->
        <item name="colorPrimary">@color/purple_200</item>
        <item name="colorPrimaryVariant">@color/text_view_background_color</item>
        <item name="colorOnPrimary">@color/black</item>
        <!-- Secondary brand color -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_200</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
        <!-- Overflow menu -->
        <item name="actionOverflowMenuStyle" >@style/overflow_menu</item>
        <item name="toolbarStyle">@style/toolbar</item>
        <item name="actionBarStyle">@style/action_bar</item>

    </style>
    <!-- Dialog-->
    <style name="DialogTheme" parent="Theme.AppCompat.Light.Dialog.Alert">
        <!-- Title text color -->
        <item name="android:textColorPrimary">@color/white</item>
        <item name="android:background">@drawable/bg_dialog</item>
        <item name="color">@color/dialog_bg_color</item>
    </style>
    <!-- Tool bar -->
    <style name="toolbar" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <item name="android:background">@color/toolbar_background_color</item>
    </style>
    <!-- SETTINGS ACTIVITY -->
    <!-- Toolbar style for setting activity -->
    <style name="AppTheme.Settings" parent="Theme.AppCompat.NoActionBar"></style>

</resources>
